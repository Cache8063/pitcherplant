# GC per-IP state files older than 30 days so $state_dir doesn't grow unbounded.
# Type 'e' on an existing directory cleans contents matching the age spec without
# removing the directory itself.
# Path is templated by install.sh.
e __STATE_DIR__ - - - 30d
